repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.12
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-added-large-files
        args: ['--maxkb=1000']  # Allow test cassettes up to 1MB
      - id: check-merge-conflict
      - id: check-json

  # Mypy disabled in pre-commit - use `nox -s mypy` instead for full environment
  # - repo: https://github.com/pre-commit/mirrors-mypy
  #   rev: v1.8.0
  #   hooks:
  #     - id: mypy
  #       args: [--strict]
  #       additional_dependencies: []
  #       exclude: ^(noxfile\.py|.*/tests/.*\.py|.*/docs/.*|.*/setup\.py)$
